{
  "name": "geometry-survivor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:browser": "npm run build:local && playwright test",
    "build": "npm run build:local",
    "build:local": "npm run typecheck && npm run test && vite build --mode development",
    "build:poki": "npm run typecheck && npm run test && vite build --mode poki",
    "build:crazygames": "npm run typecheck && npm run test && vite build --mode crazygames",
    "preview": "vite preview",
    "validate": "npm run typecheck && npm run test && npm run build:local"
  },
  "dependencies": {
    "howler": "^2.2.4",
    "pixi.js": "8.20.0",
    "zzfx": "^1.3.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/howler": "^2.2.13",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0",
    "vitest": "^3.2.0"
  }
}
